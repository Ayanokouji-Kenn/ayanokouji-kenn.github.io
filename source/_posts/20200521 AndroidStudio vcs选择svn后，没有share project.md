项目路径/.idea/vcs.xml中
```
<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="VcsDirectoryMappings">
<!-- 将这里的 vcs="svn"中的svn去掉，留个空串，再回as看有没有出现share project -->
    <mapping directory="" vcs="" />   
  </component>
</project>
```